import { useLocalSearchParams, useRouter } from 'expo-router';
import { useSQLiteContext } from 'expo-sqlite';
import { useState } from 'react';
import { KeyboardAvoidingView, Platform, Pressable, ScrollView, StyleSheet, Text, TextInput, View } from 'react-native';
import { colors, radius, spacing } from '../constants/theme';
import { titleCase } from '../lib/format';
import type { WorkLogType } from '../types';

const types: WorkLogType[] = ['code', 'bug_fix', 'investigation', 'testing', 'refactoring', 'documentation', 'decision', 'other'];
export default function LogProgressScreen() { const { ticketId } = useLocalSearchParams<{ ticketId: string }>(); const router = useRouter(); const db = useSQLiteContext(); const [description, setDescription] = useState(''); const [remains, setRemains] = useState(''); const [nextAction, setNextAction] = useState(''); const [commit, setCommit] = useState(''); const [files, setFiles] = useState(''); const [type, setType] = useState<WorkLogType>('code'); const [saving, setSaving] = useState(false); const save = async () => { if (!description.trim() || saving) return; setSaving(true); try { const now = new Date().toISOString(); await db.runAsync('INSERT INTO work_logs (ticket_id, type, description, what_remains, next_action, commit_hash, created_at) VALUES (?, ?, ?, ?, ?, ?, ?)', Number(ticketId), type, description.trim(), remains.trim(), nextAction.trim(), commit.trim(), now); for (const path of files.split(/[\n,]/).map(x=>x.trim()).filter(Boolean)) await db.runAsync('INSERT INTO ticket_files (ticket_id, file_path, created_at) VALUES (?, ?, ?)', Number(ticketId), path, now); await db.runAsync("UPDATE tickets SET status = CASE WHEN status = 'paused' THEN 'in_progress' ELSE status END, next_action = ?, updated_at = ? WHERE id = ?", nextAction.trim(), now, Number(ticketId)); router.back(); } finally { setSaving(false); } }; return <KeyboardAvoidingView style={styles.page} behavior={Platform.OS === 'ios' ? 'padding' : undefined}><View style={styles.nav}><Pressable onPress={() => router.back()}><Text style={styles.cancel}>Cancel</Text></Pressable><Text style={styles.title}>Log progress</Text><Pressable onPress={save} disabled={!description.trim()}><Text style={[styles.save, !description.trim() && styles.disabled]}>Save</Text></Pressable></View><ScrollView contentContainerStyle={styles.content} keyboardShouldPersistTaps="handled"><Text style={styles.intro}>A small note now makes the next session easier.</Text><Text style={styles.label}>What did you do? <Text style={styles.required}>Required</Text></Text><TextInput autoFocus multiline value={description} onChangeText={setDescription} placeholder="Describe the progress in a sentence or two..." placeholderTextColor={colors.muted} style={[styles.input, styles.large]} /><Text style={styles.label}>Type</Text><ScrollView horizontal showsHorizontalScrollIndicator={false} style={styles.types}>{types.map((item) => <Pressable key={item} onPress={() => setType(item)} style={[styles.type, type === item && styles.typeSelected]}><Text style={[styles.typeText, type === item && styles.typeTextSelected]}>{titleCase(item)}</Text></Pressable>)}</ScrollView><Text style={styles.label}>What remains?</Text><TextInput multiline value={remains} onChangeText={setRemains} placeholder="Anything unfinished or worth remembering..." placeholderTextColor={colors.muted} style={[styles.input, styles.medium]} /><Text style={styles.label}>Next action</Text><TextInput value={nextAction} onChangeText={setNextAction} placeholder="What will you do next?" placeholderTextColor={colors.muted} style={styles.input} /><Text style={styles.label}>Files changed <Text style={styles.optional}>Optional</Text></Text><TextInput value={files} onChangeText={setFiles} multiline placeholder="src/cache.ts, auth-service.ts" placeholderTextColor={colors.muted} style={[styles.input, styles.medium]} /><Text style={styles.label}>Commit hash <Text style={styles.optional}>Optional</Text></Text><TextInput value={commit} onChangeText={setCommit} autoCapitalize="none" placeholder="e.g. a82cf1d" placeholderTextColor={colors.muted} style={styles.input} /><Pressable style={[styles.button, !description.trim() && styles.buttonDisabled]} onPress={save} disabled={!description.trim()}><Text style={styles.buttonText}>{saving ? 'Saving...' : 'Save progress'}</Text></Pressable></ScrollView></KeyboardAvoidingView>; }
const styles = StyleSheet.create({ page: { flex: 1, backgroundColor: colors.canvas }, nav: { flexDirection: 'row', justifyContent: 'space-between', alignItems: 'center', paddingHorizontal: spacing.lg, paddingTop: 14, paddingBottom: 13, borderBottomColor: colors.line, borderBottomWidth: 1 }, cancel: { color: colors.muted, fontSize: 14 }, title: { color: colors.ink, fontWeight: '800', fontSize: 16 }, save: { color: colors.green, fontWeight: '800', fontSize: 14 }, disabled: { color: colors.muted }, content: { padding: spacing.lg, paddingBottom: 40 }, intro: { color: colors.muted, fontSize: 14, marginBottom: 24 }, label: { color: colors.ink, fontWeight: '800', fontSize: 13, marginTop: 15, marginBottom: 8 }, required: { color: colors.green, fontSize: 11 }, optional: { color: colors.muted, fontSize: 11 }, input: { backgroundColor: colors.surface, borderColor: colors.line, borderWidth: 1, borderRadius: radius.md, color: colors.ink, fontSize: 15, paddingHorizontal: 13, paddingVertical: 12 }, large: { minHeight: 112, textAlignVertical: 'top' }, medium: { minHeight: 82, textAlignVertical: 'top' }, types: { flexGrow: 0 }, type: { backgroundColor: colors.surface, borderColor: colors.line, borderWidth: 1, borderRadius: radius.pill, paddingHorizontal: 13, paddingVertical: 9, marginRight: 7 }, typeSelected: { backgroundColor: colors.charcoal, borderColor: colors.charcoal }, typeText: { color: colors.muted, fontSize: 12, fontWeight: '700' }, typeTextSelected: { color: '#fff' }, button: { backgroundColor: colors.green, borderRadius: radius.md, alignItems: 'center', paddingVertical: 15, marginTop: 28 }, buttonDisabled: { backgroundColor: colors.line }, buttonText: { color: '#fff', fontWeight: '800' } });
